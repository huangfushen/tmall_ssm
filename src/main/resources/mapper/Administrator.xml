<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

	<mapper namespace="com.tmall.mapper.AdministratorMapper">
	    <select id="getByName"  parameterType="string" resultType="Administrator">
	        select * from administrator where name = #{0}
	    </select>
<!--	<select id="getByNameAndPass"  parameterType="string" resultType="BaseResultMap">
	        select * from administrator where name = #{0} and password = #{1}
	    </select>-->
	</mapper>

